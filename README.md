# 🎓 پلتفرم مدیریت هوشمند دانشگاه – فاز ۱ (کشف و بنیان‌گذاری)

## ۱. مقدمه
پروژه «پلتفرم مدیریت هوشمند دانشگاه» با هدف طراحی و توسعه یک سامانه یکپارچه برای مدیریت فرآیندهای آموزشی، خدماتی و عملیاتی دانشگاه‌ها ایجاد شده است.  
این پروژه بخشی از درس **تحلیل و طراحی سیستم‌ها** است و تمرکز آن بر یادگیری عملی مفاهیم معماری نرم‌افزار به‌ویژه **معماری میکروسرویس (Microservices Architecture)** می‌باشد.  

سیستم نهایی باید قابلیت مقیاس‌ پذیری، امنیت، چند مستأجری (Multi-tenancy) و پایداری بالا را داشته باشد و از الگوهای **Saga، Circuit Breaker، Event-driven messaging (RabbitMQ)** و **API Gateway** استفاده کند.

---

## ۲. دامنه و اهداف سیستم (Scope)
هدف سیستم، ایجاد یک بستر هوشمند برای مدیریت تعاملات بین دانشگاه، دانشجو و استاد است. کاربران اصلی سیستم شامل موارد زیر هستند:

| نقش | وظایف کلیدی |
|-----|---------------|
| 🎓 دانشجو | ثبت‌نام، ورود، مشاهده‌ی منابع، رزرو، شرکت در آزمون، خرید خدمات آموزشی |
| 👨‍🏫 استاد | طراحی آزمون، برگزاری کلاس، ارائه دوره |
| 🏛️ مدیر دانشکده | مدیریت منابع، نظارت، افزودن محصولات (کارگاه‌ها، خدمات) |
| 🖥️ سیستم مرکزی | هماهنگی بین ماژول‌ها و تضمین سازگاری داده‌ها |

فعالیت‌های اصلی سیستم:
- مدیریت کاربران (ثبت‌نام و احراز هویت با JWT)  
- مدیریت منابع قابل رزرو (اتاق‌ها، تجهیزات)  
- بازارچه خدمات و رویدادهای دانشگاهی (کارگاه، بلیط، خدمات آموزشی)  
- آزمون و آموزش آنلاین  
- مانیتورینگ IoT (سنسور دما و موقعیت شاتل‌ها در محیط دانشگاه)

---

## ۳. معماری کلی سیستم
سیستم بر مبنای معماری **میکروسرویس‌های مستقل با ارتباط رویدادمحور** طراحی می‌شود.  
هر سرویس وظیفه‌ی خاص خود را دارد و از طریق پیام‌های RabbitMQ و Gateway API با سایر سرویس‌ها در ارتباط است.

### 🧩 سرویس‌های اصلی
| نام سرویس | عملکرد کلیدی | فناوری / الگو |
|------------|---------------|----------------|
| **User & Auth Service** | مدیریت ثبت‌نام، ورود و صدور توکن JWT | Spring Boot + JWT + MySQL |
| **Resource & Booking Service** | نمایش و رزرو منابع دانشگاهی (اتاق، آزمایشگاه) | Spring Boot + Saga Pattern |
| **Marketplace Service** | بازارچه خدمات (فروش بلیط کارگاه‌ها) | Saga + RabbitMQ + Circuit Breaker |
| **E‑Learning & Exam Service** | ثبت آزمون، شرکت در آزمون، ارسال رویداد شروع | Spring Boot + Circuit Breaker Pattern |
| **IoT Monitoring Service** | دریافت اطلاعات سنسورها (دما، موقعیت شاتل) و ارسال به داشبورد | WebSocket + MQTT (模‌سازی‌شده) |
| **API Gateway** | نقطه ورود یکپارچه برای کلاینت‌ها و کنترل دسترسی | Spring Cloud Gateway |
| **Config Server / Discovery Service** | مدیریت تنظیمات و کشف سرویس‌ها | Spring Cloud Config + Eureka |

---

## ۴. تصمیمات معماری اولیه (ADRs)
| شناسه | تصمیم | توجیه |
|--------|--------|--------|
| **ADR‑01** | استفاده از معماری میکروسرویس | استقلال استقرار، مقیاس‌پذیری افقی |
| **ADR‑02** | استفاده از RabbitMQ برای ارتباطات غیرهمزمان | حذف وابستگی بین سرویس‌ها و تضمین تاب‌آوری |
| **ADR‑03** | استفاده از Saga در تراکنش‌های بین‌سرویسی مانند خرید | کنترل تراکنش‌های توزیع‌شده |
| **ADR‑04** | استفاده از Circuit Breaker در ارتباطات Marketplace → Notification | جلوگیری از اثر آبشاری خرابی |
| **ADR‑05** | احراز هویت با JWT و پیاده‌سازی نقش‌ها (RBAC) | امنیت و جداسازی سطح دسترسی |
| **ADR‑06** | طراحی Multi‑Tenant با Schema‑per‑Tenant | ایزوله‌سازی داده‌ها بین دانشکده‌ها |
| **ADR‑07** | استفاده از Redis برای کشینگ دیتاهای خواندنی مکرر | بهبود سرعت پاسخگویی < 400 ms |

---

## ۵. نمودارهای فاز ۱

### 📘 Context Diagram
> نمایش محیط کلی سیستم و تعامل سرویس‌ها با کاربران اصلی و سامانه‌های خارجی (Gateway، دیتابیس، پیام‌بر).

### 📗 Use Case Diagram
> نمایش رفتارهای کلیدی کاربران: ثبت‌نام، رزرو، خرید، ایجاد آزمون، شرکت در آزمون.

### 📕 Initial Component Diagram
> نمایش اجزای اولیهٔ میکروسرویس‌ها و نوع ارتباط آن‌ها (Sync/Async، از طریق Gateway و RabbitMQ).

*(دیاگرام‌ها در مسیر `/docs/phase1-diagrams/*.png` قرار می‌گیرند.)*

---

## ۶. نتیجه‌گیری (Outcome)
این فاز پایه‌ی شناخت و طراحی سیستم را تشکیل می‌دهد. تصمیمات معماری، محدوده‌ی عملکرد و ساختار میکروسرویس‌ها مشخص شده و زیرساخت‌ لازم برای فاز ۲ (ساخت هسته اولیه) آماده است.

---
